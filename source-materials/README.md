# Source Materials

Upload the original Drive materials here.

Do not separate lecture decks by extension. PPTX, PPSM, and PDF presentations all belong in `presentations/`, grouped by topic.

Use `notebooks/` only for confirmed `.ipynb` files from the Drive materials. Use `project-documents/` for project roles/protocol/rationale documents.

Do not add inferred script names here unless you actually have those files and intentionally want to preserve them.
