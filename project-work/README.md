# Project Work

This folder contains the final A2A receptor MD project summary files.

Use `source-materials/presentations/10-project-a2a-md/` for `A2A apo MD Slides.pptx`.
Use `source-materials/project-documents/` for `delegation`, `protocol tracking document.docx`, and `project outline and rationale`.

`CompBioprojectpitch.pptx` is intentionally excluded from the upload map for this repo version.
